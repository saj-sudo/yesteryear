import { render } from 'preact';

// Placeholder app shell — replaced when the app shell + views land.
function App() {
  return (
    <main class="landing">
      <h1>Yesteryear</h1>
      <p>App under construction.</p>
    </main>
  );
}

render(<App />, document.getElementById('root')!);
